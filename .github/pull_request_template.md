## What does this change?

<!-- A short description. Link the issue if there is one: "Fixes #123". -->

## Checklist

- [ ] `go test ./...` passes (run from `game-launcher/`)
- [ ] New user-facing strings are added to **all** `frontend/src/locales/*.json`
- [ ] UI changes: a screenshot is attached (covers blurred with `Ctrl+H`)
